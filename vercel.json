{
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "regions": ["iad1"],
  "env": {
    "NEXT_PUBLIC_SERVER_URL": {
      "description": "Backend server URL including /api/auth path for Better Auth",
      "required": true
    }
  }
}
